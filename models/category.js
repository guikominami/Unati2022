class Category {
    constructor(id, title, color, page, [type]) {
      this.id = id;
      this.title = title;
      this.color = color;
      this.page = page;
      this.type = type
    }
  }
  
  export default Category;
  